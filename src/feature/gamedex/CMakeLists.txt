find_package(PkgConfig REQUIRED)
pkg_check_modules(GAMEDEX_AV REQUIRED IMPORTED_TARGET libavcodec>=59 libavformat libavutil libswscale libswresample)
add_library(mgba-gamedex STATIC capture.cpp)
target_compile_features(mgba-gamedex PRIVATE cxx_std_17)
target_compile_definitions(mgba-gamedex PRIVATE ${FEATURE_DEFINES} ${FUNCTION_DEFINES} ${OS_DEFINES})
target_link_libraries(mgba-gamedex PUBLIC ${BINARY_NAME} PkgConfig::GAMEDEX_AV)
add_executable(gamedex-capture-test capture-test.cpp)
target_compile_features(gamedex-capture-test PRIVATE cxx_std_17)
target_compile_definitions(gamedex-capture-test PRIVATE ${FEATURE_DEFINES} ${FUNCTION_DEFINES} ${OS_DEFINES})
target_link_libraries(gamedex-capture-test PRIVATE mgba-gamedex)
